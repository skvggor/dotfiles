bower
browserify
dev-time-cli
empty-trash
grunt
grunt-cli
gulp
hicat
jit-grunt
mocha
n
node-sass
npm
stylus
svgo
yo
# sudo npm -g -p --depth 0 ls | awk -F "/" '{if (NR > 1) print $NF}' > packages; and echo \# (history | grep -m 1 npm) >> packages
